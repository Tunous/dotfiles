#!/bin/bash

if [[ "$1" == "--no-input" ]]; then
    noinput="-noinput"
    shift
else
    input=$(cat)
fi

prompt="$@"
echo "$input" | dmenu -b -i -nf '#9e9e9e' -nb '#000000' -sf '#000000' -sb '#0087af' -p "$prompt" $noinput
