#!/bin/bash

if [[ "$1" == "--no-input" ]]; then
    noinput="-noinput"
    shift
else
    input=$(cat)
fi

prompt="$@"
echo "$input" | dmenu -b -i -nf '#aaaaaa' -nb '#0a0f14' -sf '#0a0f14' -sb '#33859d' -p "$prompt" $noinput
